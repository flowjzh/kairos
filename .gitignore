# macOS
.DS_Store

# Swift / Xcode
.build/
.swiftpm/
DerivedData/
build/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/

# Package artifacts
Package.resolved

# Dashboard web app (Vite build output + deps)
dashboard/dist/
dashboard/node_modules/
dashboard/*.tsbuildinfo

# Staged C-ABI staticlib (produced by `make ffi`)
daemon/mac/lib/

# Local runtime data (never commit)
~/.kairos/
*.db
*.db-wal
*.db-shm

# Python (sdk/python)
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
uv.lock

# Rust (root Cargo workspace) — Cargo.lock is committed (binary crates)
target/

# Editor
.vscode/
.idea/

# Claude Code local tooling (skills, project plugin config)
.agents/
.claude/
