[package]
name = "kairos-codec"
version.workspace = true
edition.workspace = true
description = "Canonical Kairos line-JSON wire protocol (Rust canonical; Swift daemon mirrors)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
